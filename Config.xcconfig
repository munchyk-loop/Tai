// Some of the items can be modified to match the user's preference
APP_DISPLAY_NAME = TAI
// APP_ICON = taiBlack
APP_URL_SCHEME = Tai

// DEVELOPER_TEAM will be set to your Apple Developer ID - typically using ConfigOverride.xcconfig
DEVELOPER_TEAM = ##TEAM_ID##

// Typically this is not modified unless you want to create a separate (unique) app using your ID
// It must include $(DEVELOPMENT_TEAM)
// Tai uses .tai at the end instead of .trio
BUNDLE_IDENTIFIER = org.nightscout.$(DEVELOPMENT_TEAM).tai

// Danger zone - do not modify these unless you know what you are doing

// The TAI_APP_GROUP_ID should not be modified - it is required to have this exact format
// to build with GitHub actions and to work with xDrip4iOS
TAI_APP_GROUP_ID = group.org.nightscout.$(DEVELOPMENT_TEAM).tai.tai-app-group

// The developers set the version numbers, please leave them alone
APP_VERSION = 0.8.4
APP_DEV_VERSION = 0.8.4.80
APP_BUILD_NUMBER = 1
COPYRIGHT_NOTICE = autoISF 3.01

// GitHub repo the in-app release notes are read from. Tai publishes its own
// releases; pointing this at nightscout/Trio would show Trio's notes instead.
RELEASE_NOTES_REPO = mountrcg/Tai

// Optional overrides - these can be used to insert your TEAMID into the DEVELOPER_TEAM field
#include? "../../ConfigOverride.xcconfig"
#include? "../ConfigOverride.xcconfig"
#include? "ConfigOverride.xcconfig"
